{
  "name": "ritmoflow",
  "version": "1.0.0",
  "private": true,
  "description": "RITMOFLOW - Academia de baile en línea - Proyecto Final DS1",
  "scripts": {
    "install:all": "pip install -r requirements.txt && cd frontend && npm install",
    "seed": "python3 manage.py seed_data",
    "dev:backend": "python3 manage.py runserver",
    "dev:frontend": "cd frontend && npm run dev",
    "migrate": "python3 manage.py migrate",
    "build": "cd frontend && npm run build"
  }
}
